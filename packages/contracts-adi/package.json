{
  "name": "contracts-adi",
  "version": "0.1.0",
  "description": "Generic EVM Solidity contracts and tests (Layer 2)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "demo:lock": "hardhat run scripts/demo-lock-asset.js",
    "deploy:testnet": "hardhat run scripts/deploy-testnet.js --network adiTestnet",
    "lock:live": "hardhat run scripts/lock-live-asset.js --network adiTestnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^2.26.3"
  }
}
